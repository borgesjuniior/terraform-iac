# Este arquivo define as variáveis de entrada para o módulo S3, permitindo a personalização do nome do bucket.
# Define a variável bucket_name, que é passada do módulo raiz.
variable "bucket_name" {
  type = string
}

